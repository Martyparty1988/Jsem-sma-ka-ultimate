{
  "name": "jsem-smazka-ultimate",
  "version": "1.0.0",
  "description": "Jsem smažka? – web app připravená pro iOS wrapper přes Capacitor.",
  "private": true,
  "scripts": {
    "build": "node scripts/prepare-capacitor-assets.mjs",
    "cap:add:ios": "npm run build && npx cap add ios",
    "cap:sync:ios": "npm run build && npx cap sync ios",
    "cap:open:ios": "npx cap open ios",
    "ios:prepare": "npm install && npm run build && npx cap add ios && npx cap sync ios",
    "ios:open": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}

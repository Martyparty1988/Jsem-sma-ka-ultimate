{
  "name": "jsem-smazka-ultimate",
  "private": true,
  "type": "module",
  "scripts": {
    "audit:face-reactor": "node scripts/audit-face-reactor.mjs . --strict",
    "test": "node --test tests/*.test.mjs",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1"
  }
}
